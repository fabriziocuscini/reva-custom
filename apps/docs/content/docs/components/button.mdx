---
title: Button
description: A button component with solid, outline, and ghost variants.
---

import ButtonDefault, { code as defaultCode } from '@/examples/button/default'
import ButtonVariants, { code as variantsCode } from '@/examples/button/variants'
import ButtonSizes, { code as sizesCode } from '@/examples/button/sizes'

<ComponentPreview code={defaultCode}>
  <ButtonDefault />
</ComponentPreview>

## Usage

```tsx
import { Button } from '@reva/ui'
```

## Examples

### Variants

The Button supports three visual styles: `solid` (default), `outline`, and `ghost`.

<ComponentPreview code={variantsCode}>
  <ButtonVariants />
</ComponentPreview>

### Sizes

Three sizes are available: `sm`, `md` (default), and `lg`.

<ComponentPreview code={sizesCode}>
  <ButtonSizes />
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'solid' \| 'outline' \| 'ghost'` | `'solid'` | Visual style |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Button size |
| `className` | `string` | — | Additional CSS classes |
| `disabled` | `boolean` | `false` | Disable the button |
