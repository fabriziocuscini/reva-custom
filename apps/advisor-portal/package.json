{
  "name": "@reva/advisor-portal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "panda codegen --clean && panda cssgen --outfile styled-system/styles.css && tsc -b && vite build",
    "dev": "panda codegen --clean && panda cssgen --outfile styled-system/styles.css && vite",
    "preview": "vite preview",
    "codegen": "panda codegen --clean && panda cssgen --outfile styled-system/styles.css"
  },
  "dependencies": {
    "@reva/ui": "workspace:*",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@pandacss/dev": "^0",
    "@reva/panda-preset": "workspace:*",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^4",
    "typescript": "^5",
    "vite": "^6"
  }
}
